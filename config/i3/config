set $mod Mod4
set $alt Mod1
floating_modifier $mod

# safety fallback. most apps use their own fonts
# font pango:Miracode 11
font Terminess Nerd Font Mono 12

set $browser    firefox
set $term       alacritty

set $center move position center

default_border              normal 1
hide_edge_borders           none
# for_window [class="^.*"]    border pixel 0
# for_window [title="^.*"]    border pixel 0

for_window [class=".*"]      title_format "%title"
title_align center

for_window [class=".*"]      floating enable
for_window [class="krita"] floating disable
for_window [class="firefox"] floating disable
for_window [class="discord"] floating disable
for_window [class="obsidian"] floating disable
for_window [class="code"] floating disable
for_window [class="Code"] floating disable

for_window [class="feh"]        floating enable
for_window [class="wine"]       floating enable
for_window [class="Display"]    floating enable
for_window [instance='lock']    floating enable; focus floating
for_window [class="Yad"] floating enable

gaps inner 3
gaps outer 3
gaps top 3
gaps bottom 3

bindsym $mod+Return exec $term

bindsym $mod+equal scratchpad show
bindsym $mod+minus move scratchpad

bindsym $mod+Shift+q kill


set $left   h
set $down   j
set $up     k
set $right  l

bindsym $mod+$left  focus left
bindsym $mod+$down  focus down
bindsym $mod+$up    focus up
bindsym $mod+$right focus right

bindsym $mod+Left   focus left
bindsym $mod+Down   focus down
bindsym $mod+Up     focus up
bindsym $mod+Right  focus right

bindsym $mod+z      focus output left
bindsym $mod+x      focus output right

bindsym $mod+Shift+$left    move left
bindsym $mod+Shift+$down    move down
bindsym $mod+Shift+$up      move up
bindsym $mod+Shift+$right   move right

bindsym $mod+Shift+Left     move left
bindsym $mod+Shift+Down     move down
bindsym $mod+Shift+Up       move up
bindsym $mod+Shift+Right    move right

set $resize 30

mode "resize" {
    # change size bindings
    bindsym $left   resize shrink width $resize px or $resize ppt
    bindsym $down   resize grow height $resize px or $resize ppt
    bindsym $up     resize shrink height $resize px or $resize ppt
    bindsym $right  resize grow width $resize px or $resize ppt

    bindsym Left    resize shrink width $resize px or $resize ppt
    bindsym Down    resize grow height $resize px or $resize ppt
    bindsym Up      resize shrink height $resize px or $resize ppt
    bindsym Right   resize grow width $resize px or $resize ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+b split h
bindsym $mod+v split v

bindsym $mod+f fullscreen toggle

bindsym $mod+Shift+space floating toggle; $center

bindsym $mod+Shift+Tab focus mode_toggle

# mostly hardcoded for my display. change as needed
bindsym $mod+Ctrl+space floating toggle; resize set 850px 950px; $center

bindsym $mod+c $center

set $ws1 "1:www"
set $ws2 "2:sns"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

workspace $ws1 output HDMI-A-0
workspace $ws2 output DisplayPort-0
workspace $ws3 output DisplayPort-1
workspace $ws4 output HDMI-A-1

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

bindsym $mod+m move workspace to output right
bindsym $mod+n move workspace to output left

exec    picom       --config $HOME/.config/picom/picom.conf
exec    dunst
exec    unclutter


# set $back       F10
# set $pause      F11
# set $forwards   F12
# set $voldown    F7
# set $volup      F8

# bindsym $mod+F10    exec playerctl previous
# bindsym $mod+F11    exec playerctl play-pause
# bindsym $mod+F12    exec playerctl next

# depends on pulseaudio-equalizer
# bindsym $mod+F2     exec pactl set-sink-volume 0 -5%
# bindsym $mod+F3     exec pactl set-sink-volume 0 +5%

bindsym $mod+d  exec rofi -show run
bindsym $mod+s  exec rofi -show window
# bindsym $mod+e  exec rofi -show emoji -modi emoji
bindsym $mod+e exec pcmanfm
# bindsym $mod+e exec thunar
bindsym $mod+p exec --no-startup-id alacritty -e rmpc
bindsym $mod+u exec --no-startup-id nvim-gtk $HOME/Seafile/My\ Library/obsidian/obby/01\ todo/randomworklog-late-2024.txt
bindsym $mod+Shift+u exec --no-startup-id nvim-gtk $HOME/Seafile/My\ Library/obsidian/obby/01\ todo/scratch.txt
bindsym $mod+Ctrl+p exec --no-startup-id $HOME/projects/pomotroid/target/release/pomotroid

bindsym $mod+Shift+Ctrl+$alt+l exec --no-startup-id firefox linkedin.com

# bindsym $mod+p  exec i3lock-fancy-multimonitor -n -p
bindsym $mod+Shift+p exec i3lock -c 000000
exec_always $HOME/.screenlayout/layout.sh

bindsym $mod+Shift+s exec flameshot gui

# change as desired
# set $paper $HOME/resources/backgrounds/134407547_p0.jpg
set $paper $HOME/resources/backgrounds/umaroad.jpg
# set $paper $HOME/resources/backgrounds/134902318_p0.jpg
# set $paper $HOME/resources/backgrounds/FSCK0534.jpg
exec_always wal -i $paper --saturate 1 --theme dkeg-amiox -b 202122 --fg eaeaea
exec $HOME/resources/killclip.sh

exec_always seafile-applet

# exec_always --no-startup-id sleep 3 && $HOME/.config/polybar/launch.sh


# class                 border  bground text    indicator child_border
client.focused          #4C4C4C #202122 #979797 #2E9EF4   #4C4C4C
client.focused_inactive #4C4C4C #202122 #4C4C4C #484E50   #4C4C4C
client.unfocused        #222222 #333333 #888888 #292D2E   #000000
client.urgent           #2F343A #900000 #FFFFFF #900000   #900000

bar {
  status_command i3blocks -c ~/.config/i3/iblocks.ini
  position top
  # font pango:Miracode 11
  font pango:Terminess Nerd Font Mono 12

  colors {
    background #1A1A1C
    statusline #FFFFFF
    separator  #666666

    #item              border  bkgrnd  text
    focused_workspace  #1A1A1C #1A1A1C #979797
    inactive_workspace #1A1A1C #1A1A1C #4C4C4C
    urgent_workspace   #2F343A #900000 #FFFFFF
    binding_mode       #2F343A #900000 #FFFFFF
    }

}
